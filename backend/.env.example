# Pica Server configuration
# Copy to .env and edit values.

# Bind address (default: 0.0.0.0)
PICA_BIND=0.0.0.0

# Port (default: 8080)
PICA_PORT=8080

# Storage directory (default: ./storage)
# Use absolute path on Windows if you want.
PICA_STORAGE=./storage

# Optional API Key (if set, App must send X-Api-Key)
# PICA_API_KEY=change_me

# File download retries (0-10). Useful for flaky networks / CDN timeouts.
# Defaults:
# - DEFAULT=2, PICACG=2, EHENTAI=1, JM=2, HITOMI=2, HTMANGA=2, NHENTAI=3
# PICA_FILE_RETRIES_DEFAULT=2
# PICA_FILE_RETRIES_PICACG=2
# PICA_FILE_RETRIES_EHENTAI=1
# PICA_FILE_RETRIES_JM=2
# PICA_FILE_RETRIES_HITOMI=2
# PICA_FILE_RETRIES_HTMANGA=2
# PICA_FILE_RETRIES_NHENTAI=3

# File download concurrency per task (1-16). Similar to App's "下载并行".
# This affects a single comic's page downloads; different from task maxConcurrent.
# Default: 6
# PICA_FILE_CONCURRENT_DEFAULT=6
# Optional per-source overrides:
# PICA_FILE_CONCURRENT_PICACG=6
# PICA_FILE_CONCURRENT_EHENTAI=6
# PICA_FILE_CONCURRENT_JM=6
# PICA_FILE_CONCURRENT_HITOMI=6
# PICA_FILE_CONCURRENT_HTMANGA=6
# PICA_FILE_CONCURRENT_NHENTAI=6
